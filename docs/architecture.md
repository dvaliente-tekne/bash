# Architecture

This repo contains Bash utilities with a clean separation between user-facing commands (`bin/`) and reusable libraries (`lib/`). Tests are written with bats-core. CI is provided via GitHub Actions and Jenkins.
