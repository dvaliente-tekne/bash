# Contributing

1. Run `make check` locally before opening a PR.
2. Use conventional commits (e.g., `feat:`, `fix:`, `docs:`).
3. Add tests for new features.
4. Keep scripts POSIX/Bash-portable when practical.
